注意：
（1）前缀树的根节点是一个空节点，所以每次遍历的时候都是从Root根结点进行观察，所以每次角度都是从上一层层级观察当前层级，代码中的表现是node.children
 (2)每次在前缀树观察的时候都是站在当前节点的父节点进行观察,在PTOS067_MaxXOR中的表现就是在上一层的节点，使用node.children(1-bit)判断
    当前这位数是否存在不同的bit，因为转成二进制非0即1，【只要理解了这一点比左神的MaxXorWithAnElementFromArr_6的写法要简单很多】，
    这道题就是构造前缀树，然后站在上一个节点观察当前节点的事情。