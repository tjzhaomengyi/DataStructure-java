一、Flink处理迟到数据的机制
1、Flink处理迟到数据主要通过三种机制来保证正确性，
首先，Watermark机制是Flink处理迟到数据的基础。它通过在数据流中插入时间戳来追踪事件时间的进展，为系统提供处理进度的依据，有效控制窗口触发时机。
其次，allowedLatennes机制允许在窗口已经触发之后，为迟到但仍在允许延迟时间范围内的数据提供再次处理的机会。通过设置合理的允许延迟时间，可以在处理延迟＆ 计算实时性之间取得平衡。
第三，对于严重超市的数据可以使用sideOutputLateData机制，将其收集到侧输出流中。这些数据可以后续通过批处理方式与之间的结果进行合并，保证数据的完整性。
