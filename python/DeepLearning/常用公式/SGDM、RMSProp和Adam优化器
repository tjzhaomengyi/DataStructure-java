SGDM（Momentum）梯度方向左惯性累积，类比成小球滚下山，之前小球一直往右滚，则：即使当前梯度变小，仍然继续往右冲
SGDM关注堵塞是梯度本身的方向。
SGDM解决震荡问题，通过惯性平滑方向

RMSProp
因为在更新的时候gt梯度使用的是gt²，把梯度信息抹掉了，RMSProp本质是上更关注数梯度的大小而不是方向,
核心思想：梯度越大，则step变大，走的步长变小。梯度越小，更新的步长越大。
本质是自适应学习率，不同维度自动调节补长。
RMSProp解决不同方向尺度不一致的问题，通过不同维度自适应缩放

Adam优化器使用了两者 SGDM + RMSProp，同时保存方向动量和平方梯度，既有惯性又有自适应学习率